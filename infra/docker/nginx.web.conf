# Legacy filename kept for docs/watchers.
# Runtime nginx config is generated by infra/docker/web-entrypoint.sh when
# API_PROXY_UPSTREAM is set; otherwise nginx.web.noproxy.conf is used.
# See Dockerfile.web.
